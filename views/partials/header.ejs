<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Web2 NodeJS Project</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/assets/css/main.css">
  <style>
    nav {
      background-color: #222;
      color: white;
      padding: 10px 0;
      display: flex;
      justify-content: center;
      gap: 20px;
      font-size: 1rem;
    }
    nav a {
      color: white;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }
    nav a:hover {
      color: #00d1b2;
    }
  </style>
</head>
<body>
  <nav>
    <a href="/app211/database">Database</a>
    <a href="/app211/contact">Contact</a>
    <a href="/app211/crud">CRUD Menu</a>

    <a href="/">Main Page</a>
    <% if (!user) { %>
      <a href="/app211/register">Register</a>
      <a href="/app211/login">Login</a>
    <% } else { %>
      <a href="/app211/dashboard">Dashboard</a>
      <% if (user.role === 'registered') { %>
        <a href="/app211/messages">Messages</a>
      <% } %>
      <% if (user && user.role === 'admin') { %>
      <a href="/app211/admin">Admin Page</a>
      <% } %>

      <% if (user && (user.role === 'registered' || user.role === 'admin')) { %>
  <a href="/app211/messages">Messages</a>
    <% } %>
      <a href="/app211/logout">Logout</a>
    <% } %>
  </nav>
